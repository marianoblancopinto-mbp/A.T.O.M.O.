/**
 * Shim de compatibilidad. El contenido real vive en @atomo/shared.
 * Se mantiene esta ruta para no romper los imports existentes del cliente.
 * TODO (fases futuras): migrar los imports a '@atomo/shared' y borrar este shim.
 */
export * from '@atomo/shared';
